# 公开前检查记录 · 2026-09-08

## Git 历史

在提交 `bf982ca` 上对本地所有引用运行 Gitleaks 8.30.1：`gitleaks git --redact --log-opts=--all`。`git rev-list --all --count` 为 82，扫描器报告检查了其中 80 个有补丁内容的提交，约 1.59 MB。

两处 `generic-api-key` 命中均在上游提交 `00b8ee9cc583a702028f6a60db666b97dbf072ee` 的 `core/dlss.py` 第 83、84 行。人工核对为 `FSR_FILES` 中的 FSR 运行库 DLL 文件名，不是密钥。没有通过全局忽略规则隐藏命中。

另检查已跟踪文件中的密钥文件、`.env`、个人设置与日志文件名，以及常见 GitHub/OpenAI/AWS 密钥格式，未发现额外命中。此结果是工具检查与人工核对记录，不是“任何形式的敏感内容绝不存在”的保证。

## 发布材料范围

- 应用源码包由 `git archive <提交>` 生成，只包含该提交的已跟踪文件，不包含 `.git` 或本地未跟踪文件。
- 本地交接文件、下载缓存、历史构建目录、个人游戏目录不进入应用源码包。
- 依赖源码从官方固定版本地址下载，摘要与仓库清单一致才打包。
- 应用包附带许可、第三方说明、启动说明与文件清单；不能单独分发 EXE。
- 游戏、插件和 NVIDIA 模型不预装到发行 ZIP。

## 可见性确认

仓库目前保持私有，公开操作曾被自动审批阻止。转为公开会让所有人读取仓库及其全部可达历史，包括提交作者姓名／邮箱、历史文档和代码；这与只提供当前版本 ZIP 的范围不同。应在发行候选检查通过后，由仓库所有者明确确认是否公开整个仓库与历史。

社区发帖已按维护者要求暂停。本记录不构成对外发布授权。
